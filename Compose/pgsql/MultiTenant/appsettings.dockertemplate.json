{
  "Tenants": {
    "Tenant1": {
      "ConnectionStrings": {
        "EdFi_Security": "host=pb-admin-tenant1;port=${POSTGRES_PORT};username=${POSTGRES_USER};password=${POSTGRES_PASSWORD};database=EdFi_Security;application name=EdFi.Ods.WebApi;pooling=${NPG_POOLING_ENABLED};minimum pool size=10;maximum pool size=${NPG_API_MAX_POOL_SIZE_SECURITY};",
        "EdFi_Admin": "host=pb-admin-tenant1;port=${POSTGRES_PORT};username=${POSTGRES_USER};password=${POSTGRES_PASSWORD};database=EdFi_Admin;application name=EdFi.Ods.WebApi;pooling=${NPG_POOLING_ENABLED};minimum pool size=10;maximum pool size=${NPG_API_MAX_POOL_SIZE_ADMIN};"
      }
    },
    "Tenant2": {
      "ConnectionStrings": {
        "EdFi_Security": "host=pb-admin-tenant2;port=${POSTGRES_PORT};username=${POSTGRES_USER};password=${POSTGRES_PASSWORD};database=EdFi_Security;application name=EdFi.Ods.WebApi;pooling=${NPG_POOLING_ENABLED};minimum pool size=10;maximum pool size=${NPG_API_MAX_POOL_SIZE_SECURITY};",
        "EdFi_Admin": "host=pb-admin-tenant2;port=${POSTGRES_PORT};username=${POSTGRES_USER};password=${POSTGRES_PASSWORD};database=EdFi_Admin;application name=EdFi.Ods.WebApi;pooling=${NPG_POOLING_ENABLED};minimum pool size=10;maximum pool size=${NPG_API_MAX_POOL_SIZE_ADMIN};"
      }
    }
  },
  "ApiSettings": {
    "Features:11:IsEnabled":true
  }
}